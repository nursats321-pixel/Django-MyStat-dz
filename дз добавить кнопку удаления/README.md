# Домашнее задание: «Крипто-паспорт и кнопка Выхода»

Сегодня на занятии мы разобрали, как работает взрослая бесセссионная авторизация на **JWT (JSON Web Tokens)**. Мы увидели, что сервер может узнавать пользователя на лету с помощью математики, вообще не нагружая базу данных. 

Домашка лайтовая, чисто на закрепление механики работы токенов.

---

## Что нужно сделать:

### Шаг 1: Добавить кнопку «Выйти» (Уничтожение токена)
Сейчас, если вы вошли в чат, вы остаетесь там навсегда, потому что токен «сидит» в браузере. Давайте сделаем кнопку эвакуации.

1. В самый конец файла **`views.py`** добавьте этот контроллер:
   ```python
   def logout_view(request):
       response = redirect('login')          # Перенаправляем на страницу входа
       response.delete_cookie('access_token') # Удаляем токен из памяти браузера
       return response

2. В файле urls.py пропишите для него путь:
    ```python
        path('logout/', views.logout_view, name='logout'),

3. На главной странице чата (index.html) в самом верху прикрепите ссылку для выхода:
    ```python
        <a href="/logout/" style="color: red; font-weight: bold;">[ Выйти из аккаунта ]</a>
